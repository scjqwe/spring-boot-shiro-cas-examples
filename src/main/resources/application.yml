spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shiro?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&useSSL=true
    username: root
    password: suncj3690
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
  thymeleaf:
    cache: false
    mode: HTML
shiro:
  cas:
    loginUrl: https://sso.huoli.com/auth/login?service=http://localhost:8080/shiro/login
    logoutUrl: https://sso.huoli.com/auth/logout?service=http://localhost:8080/shiro/login
    successUrl: http://localhost:8080/sql-audit
    failureUrl: https://sso.huoli.com/auth/login?service=http://localhost:8080/shiro/login
    casServerUrlPrefix: https://sso.huoli.com/auth
    casService: http://localhost:8080/shiro/login